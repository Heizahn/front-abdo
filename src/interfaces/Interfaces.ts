export interface User {
    id: string;
    name: string;
    role: number;
}

export interface Client {
    id: string;
    nombre: string;
    identificacion: string;
    telefono: string;
    sector: string;
    router: string;
    ipv4: string;
    plan: string;
    saldo: string;
    estado: string;
}
