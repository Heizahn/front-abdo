version: '3'

services:
    frontend:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                VITE_HOST_API: ${VITE_HOST_API}
                VITE_HOST_BCV: ${VITE_HOST_BCV}
        ports:
            - '80:80'
        restart: unless-stopped
